source /usr/local/share/chruby/chruby.sh
source /usr/local/share/chruby/auto.sh

chruby {{ chruby_ruby_version }}

# Switch to a user and set its shell to bash.
# This works for users with a configured git-shell
alias switch='sudo su -s /bin/bash'
