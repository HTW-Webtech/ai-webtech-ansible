{% if item.value.has_key('env_vars') and item.value['env_vars'] != None %}
  {% for(k, v) in item.value['env_vars'].items() %}
export {{ k | upper }}='{{ v }}'
  {% endfor %}
{% endif %}

export GEM_HOME=/var/apps/{{ item.key}}/code/vendor/bundle/ruby/2.2.0/gems
# TODO: update w/ ruby update
