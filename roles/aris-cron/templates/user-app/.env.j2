{%- if item.value.has_key('env_vars') and item.value['env_vars'] != None -%}
  {%- for(k, v) in item.value['env_vars'].items() -%}
export {{ k | upper }}='{{ v }}'
  {%- endfor -%}
{%- endif -%}
# FIXME: Quickfix/Hack/Workaround for weird windows issues
export GEM_HOME='/opt/rubies/ruby-2.2.2/lib/ruby/gems/2.2.0'
