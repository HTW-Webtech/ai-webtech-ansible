---
- name: Load chruby in ~/.bash_rc
  lineinfile:
    line='source /usr/local/share/chruby/chruby.sh'
    dest="~{{ item.key }}/.bashrc"
  with_dict: "{{ aris_users }}"

- name: Load chrubys auto.sh in ~/.bash_rc
  lineinfile:
    line='source /usr/local/share/chruby/auto.sh'
    dest="~{{ item.key }}/.bashrc"
  with_dict: "{{ aris_users }}"

- name: Load default configured ruby version in .bashrc
  lineinfile:
    line='source ~/.bashrc.ruby-version'
    dest="~{{ item.key }}/.bashrc"
  with_dict: "{{ aris_users }}"

- name: Enable default ruby 2.2.2 via ~/.bashrc.ruby-version file
  template:
    src=user-chruby/.bashrc.ruby-version
    dest="~{{ item.key }}/.bashrc.ruby-version"
  with_dict: "{{ aris_users }}"
